using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CourseTeleport : MonoBehaviour
{
    // Start is called before the first frame update
    public int courseNumber;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
